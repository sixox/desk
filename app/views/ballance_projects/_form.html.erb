<%= form_with(model: [@project, @ballance_project], local: true) do |form| %>
<div class="modal-body">




  <div class="row">
    <% if @ballance_project.errors.any? %>
    <div class="error-area" style="color:red;">
      <h3><%= pluralize(@ballance_project.errors.count, 'error') %> prohibited your allocate from being saved.</h3>
      <ul>
        <% @ballance_project.errors.each do |error|%>
        <li>
          <%= error.full_message%>
        </li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="row">
      <div class="col-lg-6">
        <div class="d-flex justify-content-start">
          <h3>Project</h3>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="d-flex justify-content-end">
          <% if @project %>
          <h3 class="secondary"><%= @project.number %></h3>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-lg-12">


      <div class="form-group">
        <label class="mb-2 mt-3">
          Balance number
          <span class="text-danger">*</span>
        </label>
        <%= form.collection_select :ballance_id, Ballance.all, :id, :name, { prompt: 'Select Balance'}, { class: "form-select mt-2" } %>
      </div>
      <div class="form-group mb-2 mt-3">
        <label class="mb-2 mt-3">
          Quantity
          <span class="text-danger">*</span>
        </label>
        <%= form.number_field(:quantity) %>
      </div>
    </div>
  </div>
</div>

  <div class="modal-footer">
  <%= form.submit 'Create Allocate', class: 'btn btn-primary', data: { action: 'click->bs-modal#submitEnd' } %>
  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
</div>
<% end %>
