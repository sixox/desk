<div class="col-lg-10 mx-auto mt-5">
  <div class="card mt-4 mb-4 border-info">
    <div class="card-header bg-info text-white">
      <h5 class="mb-0">Assessment Form for <%= @user.name.titleize %></h5>
    </div>
    <div class="card-body">
      <div class="row">
        <div class="col-md-6">
          <p class="text-muted mb-1">Score Range</p>
          <h4 class="text-dark">1 - 10</h4>
        </div>
        <div class="col-md-6">
          <p class="text-muted mb-1">Weight Range</p>
          <h4 class="text-dark">1 - 3</h4>
        </div>
      </div>
      <div class="text-center mt-3">
        <small class="text-primary">Please provide your assessments below</small>
      </div>
    </div>
  </div>




  <%= form_with(url: update_form_assessments_path(user_id: @user.id), method: :patch, local: true) do |f| %>
  <div class="card mb-5">
    <div class="card-body">
      <div class="row">
        <div class="col-lg-6">
          <div class="d-flex justify-content-start">
            
            <h5><%= @assessment_forms_ksa.first.assessment.category_point %> POINT</h5>
          </div>
        </div>
        <div class="col-lg-6">
          <div class="d-flex justify-content-end">
            <h5>دانش و مهارت و توانایی (KSA)</h5>
          </div>
        </div>
      </div>
      <table class="table table-bordered mt-3">
        <thead>
          <tr>
            <th scope="col" class="col-1">Weight</th>
            <th scope="col" class="col-1">Score</th>
            <th scope="col" class="col-9">Definition</th>
            <th scope="col" class="col-1">Criterion</th>
          </tr>
        </thead>
        <tbody>
          <% @assessment_forms_ksa.each do |assessment_form| %>
          <tr>
            <td class="align-middle">
              <div class="form-group">
                <%= number_field_tag "assessment_forms[#{assessment_form.id}][weight]", assessment_form.weight, class: "form-control", min: 1, max: 3 %>
                <% if assessment_form.errors[:weight].any? %>
                <div class="text-danger">
                  <%= assessment_form.errors[:weight].join(", ") %>
                </div>
                <% end %>
              </div>
            </td>
            <td class="align-middle">
              <div class="form-group">
                <%= number_field_tag "assessment_forms[#{assessment_form.id}][score]", assessment_form.score, class: "form-control", min: 1, max: 10 %>
                <% if assessment_form.errors[:score].any? %>
                <div class="text-danger">
                  <%= assessment_form.errors[:score].join(", ") %>
                </div>
                <% end %>
              </div>
            </td>
            <td class="align-right">
              <%= assessment_form.assessment.definition %>
            </td>
            <td class="align-right">
              <%= assessment_form.assessment.criterion %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
  <div class="card mb-5">
    <div class="card-body">
      <div class="row">
        <div class="col-lg-6">
          <div class="d-flex justify-content-start">
            <h5><%= @assessment_forms_ksa.first.assessment.category_point %> POINT</h5>
          </div>
        </div>
        <div class="col-lg-6">
          <div class="d-flex justify-content-end">
            
            <h5>رفتارها و کردارها (EVC)</h5>
          </div>
        </div>
      </div>
      <table class="table table-bordered mt-3">
        <thead>
          <tr>
            <th scope="col" class="col-1">Weight</th>
            <th scope="col" class="col-1">Score</th>
            <th scope="col" class="col-9">Definition</th>
            <th scope="col" class="col-1">Criterion</th>
          </tr>
        </thead>
        <tbody>
          <% @assessment_forms_evc.each do |assessment_form| %>
          <tr>
            
            <td class="align-middle">
              <div class="form-group">
                <%= number_field_tag "assessment_forms[#{assessment_form.id}][weight]", assessment_form.weight, class: "form-control", min: 1, max: 3 %>
                <% if assessment_form.errors[:weight].any? %>
                <div class="text-danger">
                  <%= assessment_form.errors[:weight].join(", ") %>
                </div>
                <% end %>
              </div>
            </td>
            <td class="align-middle">
              <div class="form-group">
                <%= number_field_tag "assessment_forms[#{assessment_form.id}][score]", assessment_form.score, class: "form-control", min: 1, max: 10 %>
                <% if assessment_form.errors[:score].any? %>
                <div class="text-danger">
                  <%= assessment_form.errors[:score].join(", ") %>
                </div>
                <% end %>
              </div>
            </td>
            <td class="align-right">
              <%= assessment_form.assessment.definition %>
            </td>
            <td class="align-right">
              <%= assessment_form.assessment.criterion %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
  <div class="card mb-5">
    <div class="card-body">
      <div class="row">
        <div class="col-lg-6">
          <div class="d-flex justify-content-start">
            <h5><%= @assessment_forms_function.first.assessment.category_point %> POINT</h5>
          </div>
        </div>
        <div class="col-lg-6">
          <div class="d-flex justify-content-end">
            
            <h5>نتایج کلیدی عملکرد</h5>
          </div>
        </div>
      </div>
      <table class="table table-bordered mt-3">
        <thead>
          <tr>
            <th scope="col" class="col-1">Weight</th>
            <th scope="col" class="col-1">Score</th>
            <th scope="col" class="col-9">Definition</th>
            <th scope="col" class="col-1">Criterion</th>
          </tr>
        </thead>
        <tbody>
          <% @assessment_forms_ksa.each do |assessment_form| %>
          <tr>
            
            <td class="align-middle">
              <div class="form-group">
                <%= number_field_tag "assessment_forms[#{assessment_form.id}][weight]", assessment_form.weight, class: "form-control", min: 1, max: 3 %>
                <% if assessment_form.errors[:weight].any? %>
                <div class="text-danger">
                  <%= assessment_form.errors[:weight].join(", ") %>
                </div>
                <% end %>
              </div>
            </td>
            <td class="align-middle">
              <div class="form-group">
                <%= number_field_tag "assessment_forms[#{assessment_form.id}][score]", assessment_form.score, class: "form-control", min: 1, max: 10 %>
                <% if assessment_form.errors[:score].any? %>
                <div class="text-danger">
                  <%= assessment_form.errors[:score].join(", ") %>
                </div>
                <% end %>
              </div>
            </td>
            <td class="align-right">
              <%= assessment_form.assessment.definition %>
            </td>
            <td class="align-right">
              <%= assessment_form.assessment.criterion %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
  <%= f.submit "Submit", class: "btn btn-success" %>
  <% end %>
</div>