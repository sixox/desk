<h1>Overtime Request</h1>
<%= form_with(model: @overtime_entry, url: overtime_entries_path) do |f| %>
  <div>
    <%= f.label :date, "Date of Overtime Work" %><br>
    <%= f.date_select :date %>
  </div>
  <div>
    <%= f.label :hours, "Number of Hours" %><br>
    <%= f.number_field :hours, step: 0.5 %>
  </div>
  <div class="mb-3 form-check">
  <%= f.check_box :outside_system, class: "form-check-input" %>
  <%= f.label :outside_system, "این اضافه‌کاری خارج از سیستم/دستگاه بوده و باید به محاسبه اضافه شود", class: "form-check-label" %>
  <div class="form-text">
    اگر اضافه‌کاری داخل لاگ‌های دستگاه افتاده باشد، این گزینه را نزنید (فقط برای نمایش/توضیح استفاده می‌شود).
  </div>
</div>

  <div>
    <%= f.label :reason, "Reason/Description" %><br>
    <%= f.text_area :reason, rows: 3 %>
  </div>
  <%= f.submit "Submit Overtime Request" %>
<% end %>
